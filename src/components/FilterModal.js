import React from "react";

const FilterModal = ({ setShowModal }) => {
  return (
    <div className="max-w-sm ml-auto bg-white p-10 relative top-72 sm:top-48">
      <form>FilterModal</form>
    </div>
  );
};

export default FilterModal;
